<Page x:Class="ThinqAClient.Pages.Login.LoggingIn"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ThinqAClient.Pages.Login"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoggingIn">

    <Grid>
        <materialDesign:Card Padding="16" Margin="16,16,16,45" VerticalAlignment="Bottom" Height="100">
            <Grid>
                <TextBlock x:Name="currentAction" Style="{DynamicResource MaterialDesignTitleTextBlock}" TextAlignment="Center" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center">Logging in</TextBlock>
            </Grid>
        </materialDesign:Card>
        <ProgressBar HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="15" Margin="0,0,0,15" IsIndeterminate="True" x:Name="onePart"></ProgressBar>
        <ProgressBar HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="15" x:Name="total" Minimum="0" Maximum="100"></ProgressBar>
    </Grid>
</Page>
